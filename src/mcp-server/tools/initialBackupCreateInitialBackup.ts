/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0837d60b9abd
 */

import { initialBackupCreateInitialBackup } from "../../funcs/initialBackupCreateInitialBackup.js";
import { InitialBackupCreateRequest$zodSchema } from "../../models/initialbackupcreaterequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: InitialBackupCreateRequest$zodSchema.optional().describe(
    `The initial backup configuration.`,
  ),
};

export const tool$initialBackupCreateInitialBackup: ToolDefinition<
  typeof args
> = {
  name: "create-initial-backup",
  description: `Create initial backup

Initiates an initial backup of existing assets for the active product environment.
Backup for new uploads must be enabled on the product environment, and the requester must be permitted to
initiate a backup.
`,
  scopes: ["librarian"],
  annotations: {
    "title": "Create Initial Backup",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await initialBackupCreateInitialBackup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
