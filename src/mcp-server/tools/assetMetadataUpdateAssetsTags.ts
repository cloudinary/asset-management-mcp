/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: dda6a66bce96
 */

import { assetMetadataUpdateAssetsTags } from "../../funcs/assetMetadataUpdateAssetsTags.js";
import { ResourceType$zodSchema } from "../../models/resourcetype.js";
import { TagsUpdateRequest$zodSchema } from "../../models/tagsupdaterequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  resource_type: ResourceType$zodSchema.describe(
    `The type of resource (image, video, or raw).`,
  ),
  tags_update_request: TagsUpdateRequest$zodSchema.describe(
    `The tag command, the tags to apply, and the assets to apply them to.`,
  ),
};

export const tool$assetMetadataUpdateAssetsTags: ToolDefinition<typeof args> = {
  name: "manage-asset-tags",
  description: `Adds, removes, or replaces tags on multiple assets

Applies a tag command to the given assets, addressing them by public ID.

The number of tags multiplied by the number of public IDs must not exceed 10,000.
`,
  scopes: ["librarian"],
  annotations: {
    "title": "Manage Asset Tags",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await assetMetadataUpdateAssetsTags(
      client,
      args.resource_type,
      args.tags_update_request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
