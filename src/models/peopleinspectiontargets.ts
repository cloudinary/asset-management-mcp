/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a299c74d883d
 */

import * as z from "zod";
import { collectExtraKeys as collectExtraKeys$ } from "../lib/schemas.js";
import {
  PeopleInspectionGroup,
  PeopleInspectionGroup$zodSchema,
} from "./peopleinspectiongroup.js";

/**
 * A map of inspectable types to the items and actions to inspect.
 */
export type PeopleInspectionTargets = {
  Person?: PeopleInspectionGroup | undefined;
  AdditionalProperties?: { [k: string]: any };
};

export const PeopleInspectionTargets$zodSchema: z.ZodType<
  PeopleInspectionTargets
> = collectExtraKeys$(
  z.object({
    Person: PeopleInspectionGroup$zodSchema.optional().describe(
      "The items to inspect for a type and the actions to check against them.",
    ),
  }).describe("A map of inspectable types to the items and actions to inspect.")
    .catchall(z.any()),
  "AdditionalProperties",
  true,
);

export const PeopleInspectionTargets$zodSchemaOutbound: z.ZodType<any> = z
  .object({
    AdditionalProperties: z.record(z.string(), z.any()).optional(),
    Person: PeopleInspectionGroup$zodSchema.optional(),
  }).describe("A map of inspectable types to the items and actions to inspect.")
  .transform((v) => {
    const { AdditionalProperties, ...rest } = v;
    return {
      ...rest,
      ...AdditionalProperties,
    };
  });
