/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 652dfa488326
 */

import { assetMetadataUpdateAssetsContext } from "../../funcs/assetMetadataUpdateAssetsContext.js";
import { ContextUpdateRequest$zodSchema } from "../../models/contextupdaterequest.js";
import { ResourceType$zodSchema } from "../../models/resourcetype.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  resource_type: ResourceType$zodSchema.describe(
    `The type of resource (image, video, or raw).`,
  ),
  context_update_request: ContextUpdateRequest$zodSchema.describe(
    `The context command, the metadata to apply, and the assets to apply it to.`,
  ),
};

export const tool$assetMetadataUpdateAssetsContext: ToolDefinition<
  typeof args
> = {
  name: "manage-asset-context",
  description: `Adds or clears contextual metadata on multiple assets

Applies a contextual-metadata command to the given assets, addressing them by public ID.
`,
  scopes: ["librarian"],
  annotations: {
    "title": "Manage Asset Contextual Metadata",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetMetadataUpdateAssetsContext(
      client,
      args.resource_type,
      args.context_update_request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
