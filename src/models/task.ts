/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9609f5f4adcb
 */

import * as z from "zod";
import { GeneratedAsset, GeneratedAsset$zodSchema } from "./generatedasset.js";
import { Limits, Limits$zodSchema } from "./limits.js";
import { TaskStatus, TaskStatus$zodSchema } from "./taskstatus.js";

/**
 * The generated assets. Populated once `status` is `completed`.
 *
 * @remarks
 */
export type TaskResult = { assets: Array<GeneratedAsset> };

export const TaskResult$zodSchema: z.ZodType<TaskResult> = z.object({
  assets: z.array(GeneratedAsset$zodSchema).describe(
    "The generated assets. Currently a single asset is returned.",
  ),
}).describe("The generated assets. Populated once `status` is `completed`.\n");

/**
 * An async generation task. Returned when a generation is accepted (202)
 *
 * @remarks
 * and from GET /tasks/{task_id} as it progresses. The `result` is filled
 * in once `status` is `completed`.
 */
export type Task = {
  task_id: string;
  status: TaskStatus;
  result?: TaskResult | null | undefined;
  limits?: Limits | undefined;
};

export const Task$zodSchema: z.ZodType<Task> = z.object({
  limits: Limits$zodSchema.optional().describe(
    "Rate limit information for the account's add-on quotas.",
  ),
  result: z.lazy(() => TaskResult$zodSchema).nullable().optional().describe(
    "The generated assets. Populated once `status` is `completed`.\n",
  ),
  status: TaskStatus$zodSchema.describe(
    "The status of an async generation task.\n* `pending`: accepted and queued, not yet started.\n* `processing`: generation is in progress.\n* `completed`: generation finished; `result` is populated.\n* `failed`: generation did not complete successfully.\n",
  ),
  task_id: z.string().describe(
    "Identifier for the async generation task. Use it to poll `GET /generate/{cloud_name}/tasks/{task_id}`.",
  ),
}).describe(
  "An async generation task. Returned when a generation is accepted (202)\nand from GET /tasks/{task_id} as it progresses. The `result` is filled\nin once `status` is `completed`.\n",
);
