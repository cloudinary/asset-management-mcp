/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e640de5fb62f
 */

import { uploadConcat } from "../../funcs/uploadConcat.js";
import { ConcatRequest$zodSchema } from "../../models/concatrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConcatRequest$zodSchema.describe(`The concat request parameters.`),
};

export const tool$uploadConcat: ToolDefinition<typeof args> = {
  name: "concat",
  description: `Concatenate ordered video segments into a single MP4 asset

Accepts an ordered list of remote HTTP(S) URLs pointing to video segments
and concatenates them, remuxing the result into a single MP4 asset that is
uploaded to your product environment using the supplied upload parameters.
Concatenation and the resulting upload happen asynchronously; supply a
\`notification_url\` to be notified when the upload completes.
`,
  scopes: ["librarian", "builder"],
  annotations: {
    "title": "Concatenate Video Segments",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await uploadConcat(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
